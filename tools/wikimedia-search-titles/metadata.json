{
  "id": "wikimedia-search-titles",
  "version": "1.0.0",
  "name": "Wikimedia Title Search",
  "description": "Searches Wikimedia pages by title using full-text matching. Returns titles, descriptions, and URLs",
  "author": "Shinkai",
  "keywords": [
    "wikimedia",
    "search",
    "wikipedia",
    "titles",
    "pages",
    "full-text"
  ],
  "runner": "any",
  "operating_system": ["linux", "macos", "windows"],
  "tool_set": "",
  "configurations": {
    "type": "object",
    "properties": {
      "project": {
        "type": "string",
        "description": "Target Wikimedia project (wikipedia, wiktionary, etc). Default: wikipedia",
        "default": "wikipedia"
      },
      "language": {
        "type": "string",
        "description": "Two-letter ISO language code (en, es, fr, etc). Must be supported by project. Default: en",
        "default": "en"
      }
    },
    "required": []
  },
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Text to search for in page titles. Supports partial matches and basic search operators"
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of results to return. Range: 1-50. Default: 10",
        "minimum": 1,
        "maximum": 50,
        "default": 10
      }
    },
    "required": ["query"]
  },
  "result": {
    "type": "object",
    "properties": {
      "titles": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "title": {"type": "string", "description": "Title of the matching page"},
            "description": {"type": "string", "description": "Brief excerpt or summary of the page content"},
            "url": {"type": "string", "description": "Full URL to view the page in a web browser"}
          },
          "required": ["title", "description", "url"]
        }
      }
    },
    "required": ["titles"]
  }
}
