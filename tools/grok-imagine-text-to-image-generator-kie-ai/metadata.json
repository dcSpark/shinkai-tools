{
  "name": "Grok Imagine Text to Image Generator Kie AI",
  "description": "Tool for generating images from text prompts using the grok-imagine/text-to-image model, through the kie.ai API. It generates several images at once.\nPricing (as of November 2025): $0.02)per generation — get 6 images each time.\nGet your API key at https://kie.ai",
  "keywords": [
    "text to image",
    "image generation",
    "kie.ai",
    "grok imagine",
    "generate image"
  ],
  "version": "1.0.0",
  "author": "@@official.shinkai",
  "configurations": {
    "properties": {
      "api_key": {
        "description": "Required API key for kie.ai API authentication",
        "type": "string"
      },
      "max_duration": {
        "description": "Optional maximum duration in seconds to wait for task completion. Default: 300 (equal 5 minutes)",
        "type": "string"
      },
      "polling_interval": {
        "description": "Optional interval in seconds for polling the task status (how often to check if image generation is complete). Default: 5",
        "type": "string"
      }
    },
    "required": [
      "api_key"
    ]
  },
  "oauth": [],
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "The text prompt describing the image to generate. Max length : 5000 characters."
      },
      "filename": {
        "type": "string",
        "description": "Optional base filename (without extension); if provided, images saved as {filename}_1.png, etc.; defaults to task_id if not provided"
      },
      "aspect_ratio": {
        "type": "string",
        "description": "Optional aspect ratio of the image. Must be one of '2:3', '3:2', '1:1'. Default: 3:2",
        "default": "3:2"
      }
    },
    "required": [
      "prompt"
    ]
  },
  "result": {
    "type": "object",
    "properties": {
      "saved_images_paths": {
        "description": "List of local paths where images are saved",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "status": {
        "description": "The status of the operation",
        "type": "string"
      },
      "task_id": {
        "description": "The task ID from the API",
        "type": "string"
      },
      "temporary_image_urls": {
        "description": "List of temporary URLs for generated images",
        "items": {
          "type": "string"
        },
        "type": "array"
      }
    },
    "required": []
  },
  "sqlQueries": [],
  "sqlTables": [],
  "tools": [],
  "runner": "any",
  "operating_system": [
    "linux",
    "macos",
    "windows"
  ],
  "tool_set": ""
}