{
  "id": "macos-iterm",
  "name": "macos-iterm",
  "version": "1.0.0",
  "description": "Control iTerm via AppleScript",
  "author": "Example",
  "keywords": [
    "macos",
    "iterm",
    "terminal",
    "automation",
    "applescript"
  ],
  "runner": "any",
  "operating_system": ["linux", "macos", "windows"],
  "tool_set": "",
  "configurations": {
    "type": "object",
    "properties": {},
    "required": []
  },
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "enum": [
          "pasteClipboard",
          "runCommand"
        ],
        "description": "Action to perform: 'paste' inserts clipboard content, 'run' executes shell command in terminal"
      },
      "cmd": {
        "type": "string",
        "description": "Shell command to execute. Required for runCommand. Supports any valid shell command/script"
      },
      "new_window": {
        "type": "boolean",
        "description": "Open new terminal window for command execution. Default: false (uses active window)"
      }
    },
    "required": [
      "command"
    ]
  },
  "result": {
    "type": "object",
    "properties": {
      "result": {
        "type": "string",
        "description": "Operation result: paste confirmation, command output, or error details if operation failed"
      }
    },
    "required": ["result"]
  }
}    