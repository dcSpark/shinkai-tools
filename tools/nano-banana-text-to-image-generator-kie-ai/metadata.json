{
  "name": "Nano Banana Text to Image Generator Kie AI",
  "description": "Generates images from text prompts using the google/nano-banana model via the Kie.ai API.\nPricing (as of November 2025): ~$0.02 per image.\nGet your API key at https://kie.ai",
  "keywords": [
    "kie.ai",
    "image generation",
    "text to image",
    "nano banana",
    "generate image"
  ],
  "version": "1.0.0",
  "author": "@@official.shinkai",
  "configurations": {
    "properties": {
      "api_key": {
        "description": "Required API key for kie.ai API authentication",
        "type": "string"
      },
      "max_duration": {
        "description": "Optional maximum duration in seconds to wait for task completion. Default: 300 (equal 5 minutes)",
        "type": "string"
      },
      "polling_interval": {
        "description": "Optional interval in seconds for polling the task status (how often to check if image generation is complete). Default: 5",
        "type": "string"
      }
    },
    "required": [
      "api_key"
    ]
  },
  "oauth": [],
  "parameters": {
    "type": "object",
    "properties": {
      "output_format": {
        "type": "string",
        "description": "Optional image format. Must be 'png' or 'jpeg'. Defaults to 'png'",
        "default": "png"
      },
      "filename": {
        "type": "string",
        "description": "Optional string for base filename (without extension), defaults to None (uses task_id)"
      },
      "image_size": {
        "type": "string",
        "description": "Optional aspect ratio of the image. Must be one of '1:1', '9:16', '16:9', '3:4', '4:3', '3:2', '2:3', '5:4', '4:5', '21:9', 'auto'. Defaults to 'auto'",
        "default": "auto"
      },
      "prompt": {
        "type": "string",
        "description": "The text prompt describing the image to generate. Max length : 5000 characters."
      }
    },
    "required": [
      "prompt"
    ]
  },
  "result": {
    "type": "object",
    "properties": {
      "saved_images_paths": {
        "description": "List of local paths where images were saved",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "status": {
        "description": "Status of the operation, 'success' or error message",
        "type": "string"
      },
      "task_id": {
        "description": "The ID of the generated task",
        "type": "string"
      },
      "temporary_image_urls": {
        "description": "List of temporary image URLs from the API",
        "items": {
          "type": "string"
        },
        "type": "array"
      }
    },
    "required": [
      "temporary_image_urls",
      "task_id",
      "saved_images_paths",
      "status"
    ]
  },
  "sqlQueries": [],
  "sqlTables": [],
  "tools": [],
  "runner": "any",
  "operating_system": [
    "linux",
    "macos",
    "windows"
  ],
  "tool_set": ""
}