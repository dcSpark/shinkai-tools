{
  "name": "Ideogram V3 Remix Image Editor Kie AI",
  "description": "Tool for editing images using the Ideogram V3 Remix model via the kie.ai API. Can generate up to 4 images at once.\nPricing (as of November 2025) per image: $0.0175 for Turbo, 7 $0.035 for Balanced, or $0.05 for Quality.\nGet your API key at https://kie.ai",
  "keywords": [
    "kie ai",
    "image editing",
    "edit image",
    "image to image",
    "ideogram v3",
    "remix",
    "text to image"
  ],
  "version": "1.0.0",
  "author": "@@official.shinkai",
  "configurations": {
    "properties": {
      "api_key": {
        "description": "Required API key for kie.ai API authentication",
        "type": "string"
      },
      "max_duration": {
        "description": "Optional maximum duration in seconds to wait for task completion. Default: 300 (equal 5 minutes)",
        "type": "string"
      },
      "polling_interval": {
        "description": "Optional interval in seconds for polling the task status (how often to check if image generation is complete). Default: 5",
        "type": "string"
      }
    },
    "required": [
      "api_key"
    ]
  },
  "oauth": [],
  "parameters": {
    "type": "object",
    "properties": {
      "input_image_path": {
        "type": "string",
        "description": "Required full local path to the input image to edit. Accepted types: .jpeg, .png, and .webp. Max size : 10MB"
      },
      "image_size": {
        "type": "string",
        "description": "Optional image size. Must be one of 'square', 'square_hd', 'portrait_4_3', 'portrait_16_9', 'landscape_4_3', 'landscape_16_9'. Defaults to None (omit for API default)"
      },
      "prompt": {
        "type": "string",
        "description": "The text prompt describing how to edit the image. Max length : 5000 characters."
      },
      "input_image_strength": {
        "type": "number",
        "description": "Optional number for strength of the input image in the remix, min 0.01 max 1 step 0.01; defaults to 0.95",
        "default": 0.95
      },
      "negative_prompt": {
        "type": "string",
        "description": "Optional negative prompt to avoid in image editing. Max length : 5000 characters. Defaults to None (omit if not provided)"
      },
      "num_images": {
        "type": "integer",
        "description": "Optional number of images to generate. Must be 1-4. Defaults to 1",
        "default": 1
      },
      "seed": {
        "type": "integer",
        "description": "Optional seed (integer) for reproducible generation. Defaults to None, a random seed is chosen."
      },
      "expand_prompt": {
        "type": "boolean",
        "description": "Wether to enable automated prompt improvement (may increase processing time). Defaults to False"
      },
      "style": {
        "type": "string",
        "description": "Optional style type to generate with. Must be one of 'AUTO', 'GENERAL', 'REALISTIC', 'DESIGN'. Defaults to 'AUTO'",
        "default": "AUTO"
      },
      "filename": {
        "type": "string",
        "description": "Optional base filename for the created image(s) (without extension); defaults to None (uses task_id)"
      },
      "rendering_speed": {
        "type": "string",
        "description": "Optional rendering speed to use. Must be one of 'TURBO', 'BALANCED', 'QUALITY'. Defaults to 'BALANCED'",
        "default": "BALANCED"
      }
    },
    "required": [
      "prompt",
      "input_image_path"
    ]
  },
  "result": {
    "type": "object",
    "properties": {
      "saved_images_paths": {
        "description": "List of saved local image paths",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "status": {
        "description": "The status of the operation",
        "type": "string"
      },
      "task_id": {
        "description": "The task ID",
        "type": "string"
      },
      "temporary_image_urls": {
        "description": "List of temporary image URLs",
        "items": {
          "type": "string"
        },
        "type": "array"
      }
    },
    "required": [
      "temporary_image_urls",
      "task_id",
      "saved_images_paths",
      "status"
    ]
  },
  "sqlQueries": [],
  "sqlTables": [],
  "tools": [],
  "runner": "any",
  "operating_system": [
    "linux",
    "macos",
    "windows"
  ],
  "tool_set": ""
}